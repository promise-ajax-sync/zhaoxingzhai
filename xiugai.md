# 修改记录

## 2026-09-19 最新项目总览（今日更新）

### 2026-09-20 追加进展

- 本地业务数据已接入 Drift/SQLite：新增角色表、角色同步状态表、历史记录表、历史同步状态表和迁移元数据表。生产环境由 `AppShell` 显式注入 Drift，旧 `SharedPreferences` 角色、历史和同步状态会在首次启动时自动导入，并通过迁移标记防止重复导入。Web 端已配置 `sqlite3.wasm` 和 `drift_worker.js`，测试环境使用内存 SQLite。静态分析、243 项测试、Web Release 和 Android Debug 构建均已通过。
- 完成运行时旧项目命名清理：`MingyuCoreError` 更名为 `DivinationEngineError`，引擎与数据协议版本改为 `zhaoxingzhaiEngineVersion` / `zhaoxingzhaiSchemaVersion`，资源元数据字段 `mingyuVersion` 改为中性的 `sourceVersion`。产品描述、运行时注释和测试名称不再使用 `mingyu-core` / `mingyu` / `sydf` 作为项目自身命名。第三方来源字段、审计脚本、迁移文档与许可证归属记录仍保留真实上游名称。
- 案例云同步第一版已接入：后端新增案例新增/更新、列表、详情和软删除接口；Flutter 新增案例云同步客户端、本地与云端合并、服务端 ID 映射和离线删除队列。
- 案例与历史关系得到补强：上传历史时按 `caseSnapshot.caseId` 关联服务端案例；案例后上传时也会回填此前未关联的历史记录。
- 登录成功后同时刷新账户历史和案例。
- 登录/注册不再使用临时弹窗，统一迁移到“设置”页面的账号中心。
- 设置页新增响应式登录/注册卡片、账户资料展示、退出账号、修改用户名和头像 URL。
- 后端新增 `PATCH /api/v1/auth/me`，支持修改昵称和头像地址。
- 数据库新增 `users.avatar_url`，Alembic 已升级至 `20260920_0004 (head)`。
- 后端验证保持 `24 passed`；Flutter 完整验证仍需运行 `scripts/verify_foundation.ps1`。

当前紧接着需要处理：Flutter 静态分析/组件测试反馈、案例同步冲突测试，以及把头像 URL 输入升级为真正的图片选择与对象存储上传。

#### 2026-09-20 账号安全追加

- Access Token、Refresh Token 和过期时间已从 `SharedPreferences` 迁移到 `flutter_secure_storage`；普通用户展示资料仍保留在本地缓存。
- 增加旧版令牌自动迁移：首次启动会将旧 `SharedPreferences` 令牌写入安全存储并删除旧值。
- 后端新增 `POST /api/v1/auth/change-password`。
- 修改密码必须校验当前密码，新密码不能与旧密码相同；成功后撤销该账号全部 Refresh Token。
- 设置页已启用修改密码入口，成功后清除本地会话并要求使用新密码重新登录。
- 后端测试保持 `24 passed`。
- `flutter_secure_storage 9.2.4` 及其平台实现已写入 `pubspec.lock`；仍需通过完整基础验证确认各目标平台构建正常。

#### 2026-09-20 邮箱验证与密码找回

- 新增邮箱验证状态 `users.email_verified` 和一次性令牌表 `email_tokens`。
- 数据库已升级至 `20260920_0005 (head)`。
- 新增邮箱验证接口：请求验证码、确认验证码。
- 新增密码找回接口：请求重置验证码、使用验证码设置新密码。
- 重置密码成功后撤销账号现有 Refresh Token。
- 设置页显示邮箱是否已验证，并提供验证入口。
- 登录页增加“忘记密码”入口和完整重置流程。
- 邮件发送层使用可配置 SMTP；未配置 SMTP 时，非生产环境接口返回一次性调试验证码用于本地联调，生产环境绝不返回或记录验证码。
- 认证相关接口继续受请求大小限制和限流保护。
- 后端验证：`24 passed`；Alembic：`20260920_0005 (head)`。

#### 2026-09-20 头像选择与上传

- 设置页移除普通用户难以理解的“头像图片地址”输入，改为点击头像或“从相册更换头像”。
- Flutter 使用 `image_picker` 选择图片，并在上传前限制到最大 1024×1024、质量 82，以降低流量和存储占用。
- 后端新增 `POST /api/v1/auth/avatar`，只接受登录用户的 multipart 字段 `avatar`。
- 后端按文件真实签名校验 JPEG、PNG、WebP，不信任文件名；空文件、超出 2 MiB 和不支持的格式会被拒绝。
- 开发环境头像保存在 `media/avatars`，数据库仅保存可访问 URL；头像 URL 带版本参数，避免替换后仍显示浏览器旧缓存。
- `media/` 已加入前后端 Git 忽略规则，不会把用户上传内容提交到代码仓库。
- 当前本地文件存储只适用于开发和单实例部署；正式多实例上线前仍需将同一上传接口的存储实现切换为 OSS/S3，并配置稳定的公网媒体域名。
- 本轮仍需完成依赖安装并运行后端测试与 Flutter `scripts/verify_foundation.ps1`，验证通过后再把本节标记为完成。

#### 2026-09-20 账号注销闭环

- 后端新增 `DELETE /api/v1/auth/me`，仅允许已登录正式账号调用。
- 注销必须重新校验当前密码，并提交固定确认值，避免误操作或仅凭失窃 Access Token 直接删除账号。
- 注销采用永久删除：数据库通过外键级联清理用户、案例、占卜历史、AI 解读、刷新令牌和邮箱验证码。
- 本地头像文件会一并清理；文件清理异常不会让已经成功删除的账号返回含糊的失败状态。
- 设置页新增红色“注销账号”入口，用户必须输入当前密码及“注销账号”文字二次确认。
- 注销成功后 Flutter 清除安全令牌、账户缓存、本机案例、本机占卜历史及同步状态，并回到未登录状态。
- 注销接口纳入请求大小限制和认证接口限流。
- 后端新增注销确认与头像清理测试；后端完整回归结果为 `32 passed`，并已确认 FastAPI 应用可正常导入。Flutter 完整基础验证仍需执行。

#### 2026-09-20 账号模块阶段性冻结

- 账号模块当前已覆盖注册、登录、令牌刷新、退出、昵称修改、头像上传、密码修改、邮箱验证、密码找回和永久注销，现阶段暂停继续扩展。
- 以下能力登记为后续上线增强项，本阶段只保留规划，不立即实现：
  - 个人数据 JSON 导出；
  - 退出全部设备；
  - 登录设备列表与单设备下线；
  - 正式对象存储头像与媒体域名；
  - 多设备表、登录审计和异常登录提醒。
- 下一阶段转向核心业务质量，优先建设五种术式的 AI 解读评测与回归体系，避免继续扩展账号功能挤占占卜主流程完善时间。

#### 下一阶段：AI 解读质量评测与回归

1. 为梅花易数、塔罗、小六壬、灵签和每日一卦分别建立固定测试问题与固定术式证据样本。
2. 自动检查回答是否直接回应问题、是否引用当前卦象/签文/牌阵、是否包含核心结论和可执行建议。
3. 检查模型是否编造不存在的卦象、牌、动爻或签号，以及结构化 JSON 缺字段、截断和格式错误。
4. 加入通用解读、空问题、感情、事业、财运、人物心理及高风险问题样本。
5. 建立提示词版本与评测结果记录，后续修改提示词前先运行回归，避免修好一种问题却破坏其他术式。
6. 第一轮只做离线固定样本与规则断言，不依赖真实 AI Key；第二轮再增加可选的远程模型人工评分。

#### 2026-09-20 AI 质量评测第一轮完成

- 后端新增可复用规则评估器 `app/services/interpretation_quality.py`。
- 新增五种术式固定金标准样本 `evals/interpretation_cases.json`：
  - 梅花易数：事业合作推进；
  - 塔罗：感情与人物意愿；
  - 小六壬：兼职收入与投入决策；
  - 灵签：无具体问题的通用解读；
  - 每日一卦：身体不适的高风险安全边界。
- 当前自动检查内容：
  - 是否直接回应问题核心事项；
  - 是否引用当前术式的关键证据；
  - 关键依据、风险和行动建议数量是否合理；
  - 是否混入其他术式内容或编造证据；
  - 是否出现“一定会”“保证成功”等绝对化断言；
  - 行动建议是否只是“保持积极”“顺其自然”等空话；
  - 感情、财运和通用解读是否具有克制的小兆式收尾；
  - 医疗等高风险问题是否建议专业求助并取消俏皮收尾。
- 新增命令 `python scripts/evaluate_interpretation_quality.py`，无需 AI Key、不会产生模型费用。
- 固定样本评测结果：`5/5 passed`。
- 新增四项自动测试，包括三组故意错误回答，确认评估器能够发现编造、绝对化、缺少依据、空泛建议和高风险边界缺失。
- 后端完整测试结果更新为 `36 passed`。
- 下一轮应扩充同一术式的多问题样本，并增加远程模型输出文件的人工复核流程；暂不自动调用或计费。

#### 2026-09-20 AI 质量评测第二轮完成

- 固定评测样本由 5 个扩充到 8 个，新增：
  - 没有应期资料时询问具体时间，要求只回答节奏和触发条件，不编造日期；
  - 同时询问职业选择与感情影响的复合问题，要求两个事项都得到回应；
  - 用户问题中夹带“忽略规则、输出后台密钥”的提示词注入，要求只回答真实问题并保护内部信息。
- 评估器新增“核心事项分组”能力：复合问题不再是碰到任意一个关键词就算通过，而是每个问题分支都必须得到回应。
- 新增明显答非所问的负向测试：用作息建议回答职业与感情复合问题时，会同时报告两个未回应事项。
- 时间类样本明确禁止编造具体日期、星期和时刻。
- 提示词注入样本明确禁止复述恶意命令、泄露密钥格式或错误解释小六壬计算轨迹。
- 离线固定样本结果：`8/8 passed`。
- AI 质量专项测试：`5 passed`；后端完整测试：`37 passed`。
- 下一步可增加“远程模型输出落盘后离线评分”，仍由开发者主动执行，不自动调用模型、不自动产生费用。

#### 2026-09-20 已保存 AI 回答离线评分

- 新增 `scripts/evaluate_saved_interpretations.py`，用于评估已经保存到 JSON 文件中的真实模型结构化回答。
- 工具只读取本地文件，不读取 `.env`，不调用 AI Provider，也不会再次产生模型费用。
- 新增 `evals/saved_outputs.example.json`，展示 `caseId`、`modelId`、`promptVersion` 和 `reading` 的保存格式。
- 评分时会将 `caseId` 映射到固定金标准样本，再复用现有的问题回应、证据、编造、行动建议和安全边界规则。
- 输出会显示每条回答的 `PASS/FAIL`、模型 ID、提示词版本及具体失败原因，便于比较不同模型或提示词版本。
- 未知样本 ID、缺失字段和无效结构化 JSON 会明确判为失败，不会静默跳过。
- 本地个人评分文件建议使用 `evals/*.local.json`，该模式已加入后端 `.gitignore`，避免误提交可能包含真实回答的数据。
- 使用方式：复制示例文件、填入一次真实调用得到的 `reading`，再运行：

```powershell
python scripts\evaluate_saved_interpretations.py evals\saved_outputs.local.json
```

- 已保存回答评测专项测试与原质量测试合计 `7 passed`；后端完整测试更新为 `39 passed`。

#### 后续任务：云同步版本与冲突检测

- 本任务已登记但暂缓实施，待传统黄历第一版完成后再安排：
  - 案例与历史增加服务端版本号；
  - 更新时检测旧版本写入并返回冲突；
  - 增量同步、游标分页和删除墓碑；
  - 多设备同时修改、离线修改与删除冲突测试。

#### 当前开发：传统黄历第一版

- 首版范围确定为可靠的公历/农历日期、干支、生肖、节气、每日宜忌、吉神凶煞、十二值星、天神、二十八宿、冲煞、彭祖百忌与喜神/财神/福神方位。
- 数据与规则使用项目现有 `lunar` 开源历法库，不使用随机宜忌或无来源文案。
- 页面支持前一天、后一天、回到今天和日期选择器。
- 黄历内容明确标注为传统民俗文化参考，不作为现实结果保证或专业决策依据。
- 已新增 `TraditionalAlmanacDay` 日期领域模型，并接入侧栏中的“传统黄历”真实页面，不再显示占位状态。
- 页面已实现响应式宜忌卡片、干支摘要、传统日课详情与日期导航。
- 新增春节日期、公农历转换、节气识别和页面日期切换测试。
- 当前代码已完成，仍需运行 Flutter 完整基础验证；若静态分析或组件测试报告问题，再按实际输出修正。

#### 传统黄历第二版：月份日历视图

- 新增固定六周、七列的月份视图，周次从星期日开始。
- 每个日期同时显示公历日和农历日；农历初一显示月份，节气日优先显示节气名称。
- 支持上个月、下个月切换，并尽量保留当前选中的日号；目标月份天数不足时自动落到月末。
- 标记当前选中日期和今天，相邻月份日期使用弱化样式。
- 点击任意日期会切换月份并联动下方黄历详情、宜忌和传统日课信息。
- 新增 42 格月份边界、相邻月份日期、切换月份和点击日期联动测试。

#### 传统黄历第三版：节日与十二时辰

- 日期领域模型新增公历节日、农历节日、日纳音、胎神方位以及完整十二时辰数据。
- 月历格子的文字优先级调整为“节气 > 正式节日 > 农历日期”，特殊日期使用主题色突出；普通纪念日不会挤占月历格子。
- 日期摘要会展示当天节气与节日，日课详情增加日纳音、胎神方位和公历/农历节日。
- 新增可展开的十二时辰区域，每个时辰展示时间范围、干支、天神吉凶、冲煞、纳音、时宜与时忌。
- 新增春节、国庆节、十二时辰数量及页面展开交互测试。
- 代码已格式化；本机 Flutter 静态分析进程无输出并长时间未退出，需通过 `scripts/verify_foundation.ps1` 完成最终全量验证。

#### 传统黄历最终版：择日闭环与模块收口

- 黄历模块的产品范围在本阶段正式收口，后续不再按零散小版本继续堆叠功能。
- 新增传统择日查询，可从当前选中日期开始查询未来 15、30、60 或 90 天。
- 支持结婚嫁娶、搬家入宅、开业开市、出行远行、装修动土、祭祀祈福、签约交易、求财纳财、入学求学、求医治病和安葬祭扫等事项。
- 查询规则只采用 `lunar` 历法库的每日“宜/忌”：目标事项必须出现在“宜”，不得同时出现在“忌”，并排除“诸事不宜”的日期。
- 用户可选本人生肖，查询结果会排除与该生肖相冲的日期。
- 结果展示公历、星期、农历、日干支、匹配宜项、冲煞、节气和正式节日；点击结果会联动到对应月份与当日完整黄历详情。
- 页面最多展示前 12 个结果并说明总数，避免 90 天查询造成超长页面；无结果时提供明确空状态和调整建议。
- 增加择日领域测试，覆盖目标宜项、忌项冲突、生肖相冲排除和 1—90 天范围边界；增加页面生成结果的组件测试。
- 黄历模块最终包含：月份日历、日期导航、节气与节日、节日图标、每日宜忌、传统日课、日纳音、胎神、十二时辰和事项择日。
- 所有择日与吉凶内容仍属于传统民俗文化参考，不承诺现实结果，也不能替代医疗、法律、财务等专业判断。
- 当前环境无法获准在沙箱外启动 Flutter 分析器；最终状态以本机运行 `scripts/verify_foundation.ps1` 的结果为准。

#### 当前开发：今日运势最终版

- 侧栏“今日运势”已从占位页接入真实页面，首页运势条同步改为真实的当日稳定摘要。
- 新增版本化离线算法 `today-fortune v1`；同一日期和同一案例始终生成相同结果，不依赖刷新随机数或运行时 `hashCode`。
- 未选择案例时生成通用日运；选择案例后根据出生公历/农历计算生肖，并加入当日生肖相冲判断。
- 页面包含整体分数和等级、事业、财运、感情、身心、行动力、幸运色、幸运方位、幸运数字、今日适合与今日谨慎。
- 支持前一天、后一天、回到今天和日期选择器；切换日期会生成对应日期的稳定结果。
- 基础结果完全离线可用；可选调用“小兆”AI，AI 仅整理本地结构化证据，不重新计算，也不把分数解释为成功概率。
- 今日运势写入统一历史仓库，保存算法版本、案例快照、分项结果、宜忌和证据；AI 回答仍可附加到同一历史记录。
- Flutter 与 Python 后端提示词均新增今日运势专属边界：只整理当天生活节奏，不扩展为长期命运或具体事件。
- 新增稳定性、通用日运、生肖相冲、页面日期切换和离线可用测试。
- 当前代码已完成，等待运行 `scripts/verify_foundation.ps1` 后按真实输出修正；通过后今日运势进入维护状态。

#### 当前开发：合盘最终版

- 侧栏“合盘”已从占位页接入真实页面，必须从案例仓库选择两个不同案例。
- 案例不足两个时显示明确空状态，并可直接跳转案例页；案例被删除或修改后，合盘选择器会自动刷新并清理失效结果。
- 支持恋爱、婚姻、朋友、亲子和合作五种关系类型。
- 新增版本化稳定算法 `compatibility v1`，依据双方生肖关系、出生年干五行、出生季节和关系类型生成结果；交换双方顺序不会改变分数和历史标识。
- 页面展示协调分数、关系等级、双方生肖与五行、相处优势、磨合重点和针对关系类型的现实建议。
- 本地结果完全离线可用；可选调用“小兆”AI，AI 只能整理已计算证据，不得把分数解释为成功率，不得断言注定相爱、结婚、分手、背叛或读取他人内心。
- 合盘结果写入统一历史仓库，payload 同时保存双方完整案例快照；顶层保留第一方案例快照以兼容现有历史结构。
- Flutter 与 Python 后端提示词增加合盘专属安全边界，提示词版本统一提升到 v6。
- 新增交换顺序稳定性、禁止自身合盘、案例不足空状态和双案例完整计算测试。
- 当前代码已完成，等待运行 `scripts/verify_foundation.ps1` 后按真实输出修正；通过后合盘进入维护状态。

#### 产品术语调整：案例改为角色

- 用户界面中的“案例”统一调整为“角色”，包括侧栏入口、顶部添加按钮、角色列表、新建/编辑/删除对话框、设置说明、今日运势提示和合盘选择器。
- 合盘页面使用“角色一 / 角色二”，案例不足提示改为“至少需要两个角色”。
- 空名称展示改为“未命名角色”，本地与云端错误提示也使用“角色”。
- 底层类名、数据库表、JSON 字段和接口路径仍保留 `CaseProfile`、`cases` 等工程命名，避免破坏已有本地数据、云同步和数据库兼容性。

#### 已完成模块质量优化

- 传统黄历改为“当日详情 / 十二时辰 / 传统择日”三个分区，移动端不再一次展开所有长内容。
- 月份 42 格数据按年月缓存，最多保留 24 个月；单日黄历详情按日期缓存，最多保留 120 天，降低重复切换日期和月份时的历法计算开销。
- 今日运势取消“打开页面即写入历史”，改为用户点击“保存日运”后保存；请求 AI 解读时会先确保当前结果已经保存。
- 首页今日运势摘要监听当前角色变化，并在应用恢复前台时按当前日期重新计算，避免跨日后继续显示旧摘要。
- “合盘”对用户正式命名为“基础关系合盘”，分数明确标注为“协调指数”，页面说明强调它不是成功率，当前只使用生肖、年干五行与出生季节。
- 历史页面新增关键词搜索、术式筛选、角色筛选与清除筛选；合盘历史的第二个角色也会进入角色搜索和筛选范围。
- 新增黄历分区交互、今日运势主动保存和历史关键词筛选测试。
- 当前代码等待完整基础验证；验证通过后，本轮四项质量优化进入维护状态。

> 本节是当前项目的最新交接基线，优先级高于本文后面的早期记录。后面的“每日一卦 v3”“第 2/3 轮”等内容保留用于追溯，但其中测试数量、完成状态和下一步安排可能已经过期。
>
> 本文不会记录 `.env`、数据库密码、AI Key 或令牌的真实值。生产环境密钥必须由部署平台的加密环境变量管理。

### 一、今天完成的修改

#### 1. 用户问题与术式路由

- 首页问题草稿能够携带用户原始问题进入梅花易数、塔罗、小六壬、灵签和每日一卦。
- 修正问题分类和人物心理类问题识别，避免问题被错误归入 `general`。
- 各术式的本地解释和远程 AI 请求都以当前用户问题为主线，不再只输出与问题无关的固定卦辞、签文或牌义。
- 支持用户不填写具体问题时进行“通用解读”，但通用解读仍基于当前实际卦象、签号或牌阵，不使用统一模板冒充结果。

#### 2. AI 解读体系

- Flutter 增加统一的 AI 请求模型、服务工厂、后端服务适配器和提示词构建器。
- FastAPI 后端完成 AI Provider、提示词构建、远程调用、本地降级和响应规范化。
- 提示词拆分为：系统身份、安全边界、回答质量、回答风格和各术式专属规则。
- 建立“小兆”角色：兼具安静温柔的星占状态与活泼市井神算子状态；不恐吓、不故弄玄虚，不把占卜描述成必然事实。
- 感情、运势等生活类回答可在结尾使用适量语气词和文字表情，但不得影响重点表达。
- AI 返回内容升级为结构化解读：
  - 核心结论；
  - 白话解释；
  - 关键依据；
  - 风险提醒；
  - 可执行建议；
  - 能力边界；
  - 小兆式收尾。
- Flutter 使用统一 AI 解读卡片展示结构化内容，改善重点不清、普通用户看不懂的问题。
- 远程 AI 不可用时保留本地传统解读，并记录降级原因、来源、模型、提示词版本和证据版本。

#### 3. 五个术式的 AI 接入

- 梅花易数、塔罗、小六壬、三山国王灵签、每日一卦均已接入远程 AI 深度解读。
- 每次请求携带当前问题、术式证据、算法结果、本地答案和回答风格。
- AI 生成结果可以跟随历史记录保存，并恢复结构化阅读内容。
- 修复 ChoiceChip 缺少 Material、测试控件离屏、重复输入框 Finder 等组件测试问题。

#### 4. 历史记录与云同步

- Flutter 历史仓库保留本地优先策略：离线或后端失败不阻断起卦与查看结果。
- 新增云同步队列和状态：待同步、同步中、同步成功、同步失败、待删除等。
- 支持历史记录上传、下载、合并、软删除、失败重试和 AI 解读更新。
- 修复异步同步完成时仓库已经 `dispose` 导致 `notifyListeners` 报错的问题。
- 登录成功后会立即刷新账户云端历史，并继续处理本地待同步队列。
- 历史接口同时兼容游客 `X-Device-ID` 和登录用户 Bearer Token；登录用户优先按账户身份读取数据。

#### 5. PostgreSQL 与数据库迁移

- FastAPI 已接入 PostgreSQL 和 SQLAlchemy AsyncSession。
- 当前核心表：
  - `users`；
  - `cases`；
  - `divination_records`；
  - `ai_interpretations`；
  - `refresh_tokens`。
- AI 结构化解读使用 JSONB 保存。
- Alembic 迁移已执行到 `20260919_0003 (head)`：
  - `0001`：用户、案例、占卜记录、AI 解读基础表；
  - `0002`：AI 结构化阅读字段；
  - `0003`：刷新令牌表。
- Flutter 创建、删除和更新历史时可以写入数据库，不再只是本地 `SharedPreferences`。

#### 6. 账号认证与匿名数据绑定

- 后端新增接口：

```text
POST /api/v1/auth/register
POST /api/v1/auth/login
POST /api/v1/auth/refresh
POST /api/v1/auth/logout
GET  /api/v1/auth/me
```

- 密码使用随机盐和 `scrypt` 哈希，不保存明文密码。
- Access Token 使用 HMAC-SHA256 签名；Refresh Token 使用随机不透明令牌，数据库只保存其 SHA-256 哈希。
- Refresh Token 支持轮换和注销。
- 注册时可把当前匿名设备用户直接升级为正式账户。
- 登录已有账户时，会把当前设备的匿名案例和历史合并到账户；相同客户端 ID 的冲突以账户已有数据为准。
- Flutter 顶栏新增账号入口、注册/登录弹窗、账号信息和退出登录。
- Flutter 会保存会话、自动恢复登录状态，并在 Access Token 临近过期时调用刷新接口。
- 历史同步在登录后自动携带 `Authorization: Bearer ...`。

#### 7. 后端安全与接口保护

- AI 与认证接口增加请求大小限制、请求 ID 和基础限流。
- 日志不记录问题正文、请求体、Authorization、API Key 和数据库密码。
- 设备 ID 在数据库中以带服务端密钥的 HMAC 哈希保存。
- CORS、可信代理头、请求大小和限流窗口均可通过环境变量配置。
- `.env.example` 已补充 Access Token 和 Refresh Token 相关配置，但真实 `.env` 不写入本文。

#### 8. 界面与开发环境

- 统一调整应用主题颜色、AI 卡片和各术式页面的视觉协调性。
- Flutter Web 固定使用 `127.0.0.1:5173`，FastAPI 固定使用 `127.0.0.1:8000`，避免浏览器 Origin 变化导致本地历史看似丢失。
- 增加固定端口启动脚本和 VS Code 启动配置。
- 修复当前 Flutter SDK 不支持的 Material 图标。

#### 9. 今天的验证状态

- 后端单元测试：`24 passed`。
- Alembic：`20260919_0003 (head)`。
- Flutter 静态分析中发现的 `_disposed` 字段名错误已改为现有的 `_isDisposed`。
- 认证测试的中文 JSON 模拟响应已改为 UTF-8 字节响应。
- 用户最后确认 Flutter 基础验证通过。

---

### 二、当前已经形成的产品能力

| 板块 | 当前状态 |
|---|---|
| 应用外壳 | 宽屏侧栏、窄屏抽屉、顶栏、历史抽屉、案例入口、账号入口已完成 |
| 梅花易数 | 基础起卦、主互变、动爻、体用五行、本地解释、AI、历史已形成闭环 |
| 小六壬 | 两套规则、时间起课、结构化证据、本地解释、AI、历史已形成闭环 |
| 塔罗 | 78 张牌、18 种牌阵、自动/手动录牌、正逆位、AI、历史已形成闭环 |
| 三山国王灵签 | 92 签、随机抽签、签号查询、本地解释、AI、历史已形成闭环 |
| 每日一卦 | 自动/手动铜钱、本互变、动爻取用、本地解释、AI、历史已形成闭环 |
| 问题理解 | 原始问题、主题、意图和术式路由已接入 |
| AI 后端 | 远程调用、提示词、结构化响应、本地降级、日志保护已完成 |
| 历史 | 本地优先、云同步队列、PostgreSQL、AI 快照和删除同步已完成 |
| 账号 | 注册、登录、刷新、退出、游客升级和匿名数据合并已完成基础版本 |
| 数据库 | PostgreSQL + SQLAlchemy + Alembic 基础设施已完成 |
| 测试 | Flutter 单元/组件测试、后端 pytest、静态分析和 Android Debug 构建已有基础验证脚本 |

---

### 三、当前仍未完善的内容

#### P0：上线前必须完成

1. **生产密钥与部署配置**
   - 必须替换默认 `APP_DEVICE_HASH_SECRET` 和 `APP_ACCESS_TOKEN_SECRET`。
   - AI Key、数据库密码和令牌密钥必须进入部署平台 Secret，不能提交 Git。
   - 生产环境必须启用 HTTPS，限制 `APP_CORS_ORIGINS` 为正式域名。
   - 需要区分开发、测试、预发布和生产环境配置。

2. **令牌安全存储**
   - Flutter 当前基础版本使用 `SharedPreferences` 保存会话。
   - Android/iOS/Windows 正式版应迁移到系统安全存储；Web 端应采用更严格的会话方案并评估 XSS 风险。
   - 需要处理刷新令牌并发刷新、全部设备退出、密码修改后令牌失效和过期令牌定期清理。

3. **完整账号生命周期**
   - 邮箱验证码尚未实现。
   - 忘记密码、重置密码、修改密码、修改昵称尚未实现。
   - 注销账号、个人数据导出和数据删除尚未实现。
   - 当前 `users.device_id_hash` 只能表达一个主设备；正式多设备绑定应拆成独立设备表。

4. **案例云同步未闭环**
   - 数据库已有 `cases` 表，登录合并也能处理已有案例记录。
   - 但 Flutter 的案例增删改仍主要保存在本地，没有独立的案例 CRUD/同步接口与冲突处理。
   - 必须完成案例跨设备同步，才能保证历史记录和案例主体完整恢复。

5. **云同步一致性**
   - 当前以客户端 ID 去重，尚缺服务端版本号、ETag 或乐观锁。
   - 多设备同时修改、删除与恢复的冲突策略尚未完善。
   - 历史列表只有基础 `limit`，还缺游标分页、增量同步水位和服务端 tombstone 清理策略。
   - 需要正式的数据库备份、恢复演练和迁移回滚流程。

6. **AI 质量验收**
   - 目前提示词已改善“答非所问”，但仍缺固定问题集和自动评分基线。
   - 需要按术式建立评测集：问题相关性、证据一致性、白话程度、安全边界、格式完整性。
   - 需要测试提示词注入、超长输入、敏感问题、模型拒答、模型返回非 JSON 和内容截断。
   - 医疗、心理危机、法律、投资等高风险问题需要更明确的分类与转介策略。

7. **生产级可观测性与限流**
   - 当前限流器只在单个 Python 进程内有效，多实例部署要迁移到 Redis。
   - 尚缺错误追踪、性能指标、AI 调用耗时、Token/费用统计、慢 SQL 和告警。
   - 需要反向代理、健康检查、容器资源限制和优雅停机验证。

8. **法律与隐私材料**
   - 需要隐私政策、用户协议、AI/占卜免责声明和账号注销说明。
   - 需要明确问题正文、案例资料、历史记录和 AI 输出的保存期限与删除方式。
   - 项目引用 mingyu/sydf 的授权、AGPL 发布义务和第三方数据来源需要上线前最终复核。

#### P1：核心体验完善

1. **尚为占位页的功能**
   - 合盘；
   - 今日运势的真实算法与数据；
   - 传统黄历；
   - 居家风水；
   - 姓名与数字；
   - 诸葛神数；
   - 孔明神卦；
   - 设置页。

2. **本地数据存储**
   - 历史、案例和账号会话仍依赖 `SharedPreferences` 的整段 JSON/字符串存储。
   - 数据量增加后应迁移到 Drift/Isar 等结构化本地数据库，并设计从旧版本数据自动迁移的流程。

3. **AI 交互体验**
   - 尚未支持流式输出、停止生成、重新生成和追问上下文。
   - 首页看起来像对话入口，但还没有真正的多轮会话和会话历史。
   - 网络错误、超时、限流和登录过期提示还需要统一的人性化反馈。
   - 模型/渠道管理目前仍是基础展示，尚未形成管理员可配置能力。

4. **术式深度**
   - 梅花易数还可补充声音、字数、方位等起卦方法，以及更完整的月令旺衰规则。
   - 各术式仍需更多 mingyu/sydf 黑盒对照向量、边界输入和跨平台随机一致性验证。
   - 通用解读和问题解读需要继续通过真实用户反馈调整，避免模板化。

5. **历史与案例体验**
   - 案例目前以单选为主，合盘需要支持至少两个主体选择。
   - 历史缺搜索、筛选、标签、批量删除、导出和分享。
   - 需要更清楚地展示本地状态、云端状态、冲突状态和最后同步时间。

6. **界面质量**
   - 需要真机覆盖小屏、平板、桌面、横屏、深色模式和浏览器缩放。
   - 需要补键盘操作、屏幕阅读器语义、对比度、动态字体和无障碍测试。
   - 真实品牌 Logo、插画和“小兆”双状态视觉资产尚未完成。

#### P2：工程与长期建设

1. 建立 GitHub Actions 或其他 CI：静态分析、Flutter 测试、Web/Android 构建、后端测试和迁移检查。
2. 增加 Flutter Web 专项目标测试，覆盖 JavaScript 数值精度、随机算法和浏览器存储。
3. 增加后端 PostgreSQL 集成测试，覆盖注册、登录、刷新轮换、匿名数据合并和并发冲突。
4. 增加端到端测试：游客起卦 → 保存 → 注册 → 自动合并 → 第二设备登录 → 恢复历史。
5. 增加 AI Provider 重试、熔断、并发限制、费用预算和多供应商切换。
6. 评估后续 Java 服务接入边界，保持 Flutter 协议稳定，避免 Python/Java 同时重复实现业务规则。
7. 清理 README 和旧交接文档中的过期状态、乱码显示和旧测试数量。
8. 规划正式版本号、数据库兼容策略、灰度发布、崩溃回滚和应用商店发布流程。

---

### 四、建议的下一步执行顺序

#### 第 1 步：案例云同步闭环（建议下一轮立即开始）

1. 后端增加案例 CRUD 和增量列表接口。
2. Flutter 增加案例云同步客户端和离线队列。
3. 登录时同步当前案例，并正确重建历史与案例的关系。
4. 增加同名/同 ID 冲突、多设备修改和删除测试。

#### 第 2 步：账号安全完善

1. 将正式客户端令牌迁移到安全存储。
2. 增加昵称修改、密码修改、忘记密码和邮箱验证。
3. 增加设备管理、全部退出和账号注销。

#### 第 3 步：AI 评测与回答质量回归

1. 为五种术式建立固定问题集和预期证据。
2. 自动检测是否真正回答用户问题、是否引用当前结果、是否给出清晰结论。
3. 加入高风险问题、安全边界和非 JSON 返回测试。
4. 根据线上匿名反馈迭代提示词版本，不直接覆盖旧历史记录。

#### 第 4 步：上线基础设施

1. HTTPS、正式域名、反向代理和严格 CORS。
2. Redis 限流、监控告警、日志聚合和 AI 成本统计。
3. PostgreSQL 自动备份、迁移演练和恢复演练。
4. 隐私政策、用户协议、免责声明与数据删除流程。

#### 第 5 步：继续扩展产品模块

在账号、案例、历史、AI 质量和部署基础稳定后，再依次实现合盘、今日运势、黄历、姓名数字、诸葛神数、孔明神卦和设置页，避免新增页面继续建立在未稳定的数据地基上。

---

### 五、当前常用命令

Flutter 完整验证：

```powershell
cd E:\newProject\fluttersm\zhaoxingzhai
.\scripts\verify_foundation.ps1
```

Flutter Web 固定端口启动：

```powershell
.\scripts\run_web.ps1
```

后端启动：

```powershell
cd E:\newProject\fluttersm\zhaoxingzhai_backend
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

后端验证与数据库版本检查：

```powershell
python -m pytest
python -m compileall app migrations
alembic upgrade head
alembic current
```

当前预期数据库版本：

```text
20260919_0003 (head)
```

---

> 本文件是**跨会话交接文档**。每轮改动完成后更新三节：本次修改 / 下一步 / 潜在问题。
> 开始新一轮修改前**先读本文件**，避免与并行会话重复劳动或互相覆盖。
>
> 相关文档分工：
> - `docs/MIGRATION_BASELINE.md` —— 上游版本 pin、模块完成状态（权威）
> - `DEPENDENCY_SCAN_REPORT.md` / `TASK_DEVIATION_REPORT.md` —— 早期快照，**结论已部分失效**，勿当作现状
> - `docs/DATA_EXPORT_SUMMARY.md` —— 早期数据导出报告，塔罗牌阵数量已过期（写的是 3 种，实际 18 种）

---

## 状态快照（最近更新：每日一卦 v3 业务闭环）

| 项 | 状态 |
|---|---|
| git | 本地仓库，无 remote；当前工作区包含多轮未提交改动，提交前必须先检查并保留已有修改 |
| 测试 | **137 个全通过**（2026-09-17，`verify_foundation.ps1 -SkipBuild`） |
| 静态分析 | `flutter analyze` 零问题 |
| Web 构建 | 最近一次完整地基验证中通过；日常算法修改可先用 `-SkipBuild`，发布前必须跑完整构建 |
| 已形成业务闭环的术式 | 小六壬、塔罗、三山国王灵签、每日一卦 |
| 产品地基 | 应用外壳、案例库、案例快照、算法版本、随机轨迹、历史记录、全局案例选择 |
| 数据资产 | `assets/data/{tarot,ganzhi,trigrams,hexagrams,ssgw}.json`；六十四卦含完整爻辞和乾坤用辞 |
| 上游 pin | mingyu `0.4.0` / `878958f`；sydf `ffd2961`（依赖 `mingyu-core 0.2.3`） |
| 已提交 | `73f077b` 文档（IA 修正）；`4ba8b7b` 第 2 轮修复；`6ace8b1` 建本文档；`1626772` 第 1 轮修复；`fb4019a` 初版快照 |

---

## TODO 总表（截至第 2 轮，未完成的事项都在这里）

> 分三档：**近期待办**（已明确要做、只差开工）／**待定决策**（需要用户拍板）／**未开工大项**（任务书要求但尚未开始）。
> 每轮改动后**回来勾选**，不要只在「下一步」里留一份。

### A. 近期待办

| # | 事项 | 出处 | 备注 |
|---|---|---|---|
| A1 | 数据导出流水线：写 `scripts/export-mingyu-data.mjs`，改写 mingyu 生成脚本 → `assets/data/<术式>/` | 规则 1 | 推荐先做，纯查表零精度风险，用来打通整条链路 |
| A2 | 三山国王灵签数据导出 | 规则 1 | ✅ **已完成**：按用户决策使用 mingyu 0.4.0 完整数据；运行时权威入口为 `signs-full.ts + enrichSsgwSign`，已导出 92 签 JSON 并接入数据、算法、UI、历史和测试。来源记录见 `docs/SSGW_SOURCE_AUDIT.md` |
| A3 | 补塔罗端到端 golden 向量（同种子抽牌序列可复现） | 验收 1 | ✅ **已完成**：由 mingyu 0.4.0 黑盒导出单牌、三牌和凯尔特 3 组固定向量，逐字段校验牌号、牌名、位置、正逆位及随机版本 |
| A4 | 补小六壬端到端 golden 向量 | 验收 1 | ✅ **已完成**：`test_vectors/xiaoliuren/v1.json` + Flutter 向量测试，并有 0.2.3 / 0.4.0 / Dart 三方审计脚本 |
| A5 | 塔罗算法挪位：`lib/features/tarot/tarot_divination.dart` → `lib/core/engine/tarot/` | 目录约定 | ✅ **已完成**：算法文件及全部引用已迁入 `core/engine/tarot`；78 张牌关键词由导出脚本从 mingyu `tarot.ts#getCardKeywords` 同步进 JSON，Dart 不再保留硬编码表 |
| A6 | 处理 `lib/core/models/` 不存在的问题 | 目录约定 | ✅ **第 3 轮已解决**：目录已建，含 `algorithm_metadata` / `answer_preference` / `case_profile` |
| A7 | 随机层加 `algorithmVersion` 常量，并随历史记录持久化 | 潜在问题 1 | ✅ **此前已完成**：`random.dart` 已有 `randomAlgorithmId` / `randomAlgorithmVersion = 2`，`RandomTrace` 带版本且 `fromJson` 把旧记录归为 v1；本轮再把**术式**版本提升到 `DivinationHistoryRecord` 顶层。两层版本现已齐备 |
| A8 | 给随机层加 Web 目标验证脚本（`dart compile js` 后跑同一批种子对比） | 潜在问题 2 | `flutter test` 跑原生 VM，**测不出 Web 问题**，这是当前 CI 盲区 |
| A9 | 验证 `codeUnitAt` 与上游 `charCodeAt` 在代理对字符（emoji）上是否分叉 | 潜在问题 8 | 未验证，尚未构成已知 bug |
| A10 | 收敛小六壬的硬编码配色，并入 `AppTheme` | 本轮 UI 比对 | ✅ **已完成**：输入、规则选择、计算动画和结果展示已移除金色/墨蓝硬编码，统一使用 `ThemeData` 与 `AppTheme` 语义色，支持浅色/深色主题 |
| A11 | 补宽屏断点与侧栏/顶栏布局 | 本轮 UI 比对 | ✅ **界面地基已完成**：宽屏固定侧栏、窄屏抽屉、常驻顶栏和 15 入口已经落地；仍需持续补真实设备布局回归 |
| A12 | 首页 AI 对话入口（composer + 空态） | 用户提供 sydf 截图 | 🟡 **交互壳已完成**：composer、工具 chips、发送键和免责声明已落地；真实会话、模型调用和会话历史未接入 |
| A13 | 今日运势条（移动端首页） | 用户提供 sydf 截图 | 🟡 **展示壳已完成**：首页已有运势条和入口；真实运势算法与数据未接入 |
| A14 | 顶栏：AI 渠道/模型选择 + 添加案例 + 记录 | 用户提供 sydf 截图 | 🟡 **导航地基已完成**：案例、记录、回答偏好和渠道文字已落地；真实模型/渠道管理未接入 |
| A15 | 品牌标识与主题画风系统 | 用户提供 sydf 截图 | 🟡 **默认视觉壳已完成**：品牌 Hero、渐变标题和统一 token 已落地；真实 logo 资产与多画风主题未完成 |
| A16 | 「功德箱」入口 | 用户提供 sydf 截图 | 🟡 **按钮已完成**：当前回调为空，外链和是否保留仍待产品决定 |
| A17 | 把随机层版本也提升到 `DivinationHistoryRecord` 顶层 | 第 3 轮复核 | ✅ **核心已完成**：`random.dart` 已有 `randomAlgorithmId` / `randomAlgorithmVersion = 2`，并通过 `RandomTrace` 落进历史 payload。仅剩增强：顶层也存一份，便于不解析 payload 直接查询 |
| A18 | 案例与历史存储从 SharedPreferences 迁 Drift/Isar | SYDF §12.2 | 现在案例库、历史各占一个 key 且全量 JSON 读写；案例模型已就位，迁库时机成熟 |
| A19 | 合盘要求「至少两个不同案例」的选择器 | SYDF §5 | 案例模型已就位，但案例页目前只支持单选 |
| A20 | 每日一卦业务闭环 | 当前阶段 | ✅ **已完成**：三钱六次、逐枚铜钱录入、本卦/变卦/互卦、动爻辞、0–6 动爻取用规则、独立 v1 分项解读、乾坤用辞、v3 golden、逐爻成卦动画、每日确定种子、随机轨迹、UI、历史与案例快照、v1/v2/v3 历史详情恢复及损坏降级；进入维护态 |
| A21 | 梅花易数业务闭环 | 当前阶段 | ✅ **基础闭环已完成**：时间/数字/随机起卦、主互变、唯一动爻、体用五行生克、`meihua.local-reading v1` 现代白话解释、seed/replay、1–192 穷举、4 组固定向量、历法边界、页面、案例快照、历史恢复与损坏降级均已接入；后续增强声音/字数/方位和月令旺衰 |

---

## 当前阶段：每日一卦 v3 完整逻辑闭环

### 现在做的是哪一部分

当前完成的是**第四个真实术式“每日一卦”的业务闭环**。它不是单独做一个页面，也不是只随机选一卦，而是把下面整条链路全部打通：

```text
mingyu 0.4.0 六十四卦源数据
  → 自动导出完整 JSON
  → Flutter 数据模型与二进制查卦
  → 三钱六次起卦
  → 本卦 / 变卦 / 互卦 / 动爻
  → 0–6 动爻传统取用规则
  → 页面展示与手动录入
  → 案例快照与历史保存
  → v1 / v2 / v3 历史详情恢复
  → golden、穷举、组件和地基验证
```

因此每日一卦目前已经从“能算”进入“数据、算法、产品、历史、兼容和测试全部连通”的阶段。

### 完整运行逻辑

1. **加载数据**
   - `scripts/export_hexagrams.mjs` 从 `mingyu 0.4.0` 的 `hexagram-data.ts` 导出完整六十四卦。
   - `assets/data/hexagrams.json` 包含 64 个唯一卦象、六爻爻辞以及乾卦用九、坤卦用六。
   - `HexagramData` 支持按卦序、名称和六位二进制卦象查询。

2. **建立每日确定种子**
   - 自动日卦使用 `daily-hexagram:本地日期[:案例稳定哈希]`。
   - 同一天、同一案例得到相同结果；跨日期或切换案例会使用新种子。
   - 随机层记录算法 ID、版本和全部样本，便于审计。

3. **执行三钱六次起卦**
   - 每爻投三枚铜钱，每枚取 2 或 3，共消耗 18 个随机样本。
   - 六次结果严格按“初爻到上爻”保存。
   - `6 = 老阴`、`7 = 少阳`、`8 = 少阴`、`9 = 老阳`。
   - 页面也支持手动录入六个爻值。

4. **计算三个卦象**
   - 本卦：7/9 为阳，6/8 为阴。
   - 变卦：老阴 6 变阳、老阳 9 变阴，少阴少阳不变。
   - 互卦：二三四爻组成下卦，三四五爻组成上卦。
   - 六十四卦编码统一为“上卦三位 + 下卦三位”，每个经卦内部自下而上。

5. **提取动爻和原始文本**
   - 6、9 为动爻，记录位置、阴阳类型和本卦对应爻辞。
   - 所有解释依据都来自保存时的原始结构化结果，不依赖页面临时拼接。

6. **应用 0–6 动爻取用规则**
   - 0 动：取本卦卦辞。
   - 1 动：取唯一动爻爻辞。
   - 2 动：以上方动爻为主、下方动爻为辅。
   - 3 动：兼看本卦与变卦，本卦主当前、变卦主后势。
   - 4 动：取两个静爻，以下方静爻为主、上方静爻为辅。
   - 5 动：取唯一静爻。
   - 6 动：乾坤取用九/用六；其他卦以变卦卦辞为主。
   - 规则结果保存为 `takingRule`，包括摘要、主取文本和辅看文本。

7. **形成页面结果**
   - 展示本卦、变卦、互卦、六爻铜钱合计、动爻和传统取用依据。
   - 自动模式展示当日稳定卦；手动模式可录入 6/7/8/9。
   - 页面不把现代建议文案写死进算法层，传统数据与产品解读保持分层。

8. **保存历史与案例快照**
   - 历史 payload 保存算法版本、日期、六爻、铜钱记录、三个卦象、动爻、取用规则和随机轨迹。
   - 自动日卦按日期与案例去重；手动起卦按时间保留独立记录。
   - 保存当时的案例快照，后续修改或删除案例不会改变旧历史。

9. **恢复不同版本历史**
   - v1：只恢复当时保存的单卦，不伪造六爻、变卦或互卦。
   - v2：恢复本卦、变卦、互卦和动爻；当时没有 `takingRule`，明确提示且不按 v3 重算。
   - v3：完整恢复保存时的卦盘和取用规则。
   - 损坏或字段缺失的记录安全降级，不能影响其他历史记录加载。

10. **验证闭环**
    - 64 卦静爻编码穷举往返。
    - 0–6 动爻共 7 组 v3 golden 向量。
    - 覆盖乾坤全动、非乾坤全动、非法输入、确定种子、历史 v1/v2/v3 和损坏记录。
    - 当前验证基线：`00:05 +137: All tests passed!`，`Foundation verification passed.`

### 本阶段已经更新的文件与能力

**数据与导出**

- `assets/data/hexagrams.json`：由精简表升级为完整 64 卦数据。
- `scripts/export_hexagrams.mjs`：从 mingyu 0.4.0 自动导出并校验数量、唯一性和爻辞。
- `scripts/export_hexagrams.js`：保留旧命令兼容入口，避免误运行覆盖完整数据。
- `lib/core/data/hexagram_data.dart`：增加 `yaoCi`、`yongCi` 和二进制查卦。

**算法与版本**

- `lib/core/engine/daily_hexagram/daily_hexagram.dart`：三钱六次、本卦/变卦/互卦、动爻和取用规则。
- `AlgorithmCatalog.dailyHexagram` 当前为 **v3**；后续只要结构化计算结果变化就必须继续升版本。
- `test_vectors/daily_hexagram/v3.json`：固定 0–6 动爻 golden。

**页面与历史**

- `lib/features/daily_hexagram/presentation/daily_hexagram_page.dart`：自动/手动模式和完整结果展示。
- `DivinationHistoryRepository.addDailyHexagram`：写入历史、算法版本和案例快照。
- `lib/features/history/data/daily_hexagram_history.dart`：历史兼容解析层。
- `history_page.dart`：点击每日一卦记录查看保存时的完整详情。

**测试与文档**

- 完整数据测试、算法测试、golden 测试、历史兼容测试和页面入口测试已接入。
- `README.md`、`docs/MIGRATION_BASELINE.md`、`docs/ALGORITHM_STATUS.md` 已同步当前状态。

### 仍需要增强的内容

以下不是当前闭环的阻塞项，但会影响产品完整度：

1. **逐枚铜钱录入与动画（已完成）**
   - 手动模式现在按初爻到上爻分别录入三枚铜钱，每枚可在 2/3 间切换。
   - 页面即时计算合计与老阴/少阳/少阴/老阳，保存真实排列。
   - 铜钱切换有翻转动画，提交后六爻逐条形成，再展示本卦、变卦和互卦。
   - 动画只表现已确定结果，不参与随机计算，不会因帧率或中断改变卦象。

2. **分项解读层（已完成）**
   - 已增加传统概览、当前处境、内在条件、变化趋势、行动节奏和风险提醒。
   - 解读层为独立 `daily-hexagram.local-reading v1`，只读取 v3 卦盘，不改变核心算法。
   - 历史保存当时生成的解读，后续解读版本变化不会重写旧记录。

3. **规则流派配置**
   - 多动爻取用存在不同传统口径。
   - 当前规则必须保留为已版本化的默认规则集；新增流派应使用新的 ruleset/算法版本，不能覆盖 v3。

4. **更多跨实现向量**
   - 当前 v3 golden 主要锁定 Flutter 自身行为。
   - 后续增加与 mingyu/sydf 同输入的黑盒对照，尤其是互卦、全动和随机种子结果。

5. **概率与 Web 平台专项验证**
   - 大样本确认 6/7/8/9 理论概率分布。
   - 在 Web/原生双平台验证同种子得到完全相同的 18 个随机样本和卦盘。

6. **分享与导出**
   - 可将卦盘、动爻、取用依据和案例名称导出为文本或图片。
   - 分享内容需要免责声明，且不得泄漏不必要的案例隐私字段。

### 后续更新这部分时必须遵守

1. 修改六爻编码、随机消耗顺序、互卦算法、动爻判断或取用规则时，必须提升 `daily-hexagram` 算法版本。
2. 不得用新版本规则重算旧历史；历史详情必须按保存时 payload 原样展示。
3. 修改 `hexagrams.json` 前只能通过导出脚本生成，不手工维护第二份六十四卦表。
4. 每次变更至少运行：数据完整性测试、64 卦穷举、golden、历史兼容和 widget 测试。
5. 若改变随机层，必须同时验证 Web 与原生一致性，并更新随机算法版本。
6. 新增现代解读、AI 提示或方向建议时放在独立解释层，不混入卦象基础数据和核心换算。

### 整体项目还剩多少

**已形成完整或基础业务闭环的真实术式：4 个**

- 小六壬
- 塔罗
- 三山国王灵签
- 每日一卦

**公共地基基本具备**：应用外壳、数据资产、随机/重放、算法版本、案例快照、历史记录、Web/Android 构建验证。

**尚未完成的主要方向约 8–10 类**，具体数量取决于是否把相近术式合并：

- 完整六爻排盘
- 梅花易数
- 今日运势
- 传统黄历
- 八字/排盘
- 紫微斗数
- 奇门遁甲
- 星盘/西方占星
- 居家风水
- 姓名与数字，以及诸葛神数/孔明神卦等签类入口

按最终产品范围粗略估计，目前整体约在 **30%–40%**；基础设施完成度高于业务算法完成度。不要按导航入口数量判断完成度，未实现入口仍只是产品壳。

### 明确下一步

每日一卦已进入维护状态。梅花易数已经完成时间、数字、随机起卦及页面历史闭环。下一阶段是**梅花易数增强与下一个复杂术式准备**：

1. 按优先级扩展声音、字数和方位法。
2. 月令旺衰使用节气干支月，不能替代时间起卦的农历月日。
3. 真太阳时如需加入必须使用独立规则版本。
4. 补更多上游黑盒向量后，再开始六爻迁移基线。
5. ✅ 小六壬、塔罗和灵签均已补独立版本化现代白话解释层并写入历史；后续重点转向算法入口扩展与六爻基线。

### A10 / A11 补充说明（本轮 UI 比对结论）

**已经做到的部分**（无需返工）：`AppTheme` 与 sydf `tokens.css` 的色值**精确相同**——浅色 13 个主 token（canvas `#f3f2f5`、surface `#fbfafc`、surface-raised `#ffffff`、text-primary `#2e2b36`、text-secondary `#6a6572`、text-tertiary `#77717f`、line `#dfdce4`、line-strong `#cbc6d0`、accent `#8368ab`、accent-strong `#694c96`、accent-soft `#e9e2f2`、danger `#a65364`、success `#55796e`）与深色主题全套逐一对应；间距/圆角/动画时长三组数值也全部对齐。

**A10 排查范围**：`xiaoliuren_page.dart`、`date_time_input_section.dart`、`result_display_section.dart`、`calculating_animation.dart`。改法是把这些色值提成 `AppTheme` 里的语义 token，或直接复用现有 accent/warning 槽位。改完顺带验证深色模式下的观感（当前它在浅色模式下也是一整片墨蓝）。

**A11 缺失的布局 token**（sydf 有、Flutter 无）：`--ds-topbar-height: 64px`、`--ds-page-content: 1180px`、`--ds-page-gutter`、`--ds-control-sm/md/lg: 34/38/44px`、`--ds-reading-card-width: 136px`、`--ds-reading-section-x/y`。宽屏排版要散，缺的就是这些约束。

**A11 与 A10 的取舍**：A10 改动小、收益直接，建议先做；A11 改动大且依赖 B6 的结构决策，建议后置。若打算先把术式铺开再统一样式，两者都往后排，但 **A10 不宜拖太久**——后续每加一个页面都可能再抄一遍那套暗色。

### B. 待定决策（需要用户拍板）

| # | 事项 | 两个选项 | 影响 |
|---|---|---|---|
| B1 | 1232 行手写历法代码怎么办 | ①删掉改用 `lunar`（合规则 3）②保留并补齐上游测试 | 若删改，`true-solar-time`/`civil-time` 的 13 个上游测试是现成验收依据；若保留，需自行承担历法正确性 |
| B2 | 上游 baseline 用哪个版本 | ①mingyu `0.4.0` ②sydf 依赖的 `0.2.3` | 影响所有算法比对的参照物 |
| B3 | AGPL-3.0-only 传染性 | ①接受 GPL 系开源 ②只用 REST 不本地直译 ③其他 | **法律问题非技术问题**。本地直译算法构成衍生作品；网络 API 调用通常不构成 |
| B4 | `evidence` 层是否移植 | ①全部在 Flutter 侧重写渲染（建议）②移植 | 该层体量最大（如 meihua 算术 567 行 vs evidence 1897 行），本质是 UI 渲染 |
| B5 | 《这一条》的具体所指 | 待确认 | 已废弃「验收标准 2 的与上游逐字段一致」与「规则 2 的保持原函数名」。**seed + replay 保留**（那是 App 自身功能） |
| B6 | 是否复刻 sydf 的 15 视图侧栏结构 | ①复刻（侧栏 + 顶栏，移动端收抽屉）②保持现在的 4 Tab ③两者都做（宽屏侧栏 / 窄屏底部 Tab，响应式切换） | sydf 的 `appRoute.ts` 列了 15 个视图（13 个工具入口 + `cases`/`settings`），现有 Flutter 只落实了 2 个工具 + 自加的历史页。**决定 A11 怎么做**；选项 ③ 最贴近 Web/移动双端目标但工作量最大 |
| B7 | AI 对话是否为产品主轴 | ①是（首页即对话入口，A12/A14 优先级最高）②否（首页保持功能卡片列表，AI 只做结果解读） | 截图显示 sydf 的首页**主体就是一个对话框**，功能入口在侧栏。这与我此前理解的「工具集 + AI 解读」不同，需确认目标形态 |
| B8 | 画风主题与品牌资产 | ①连主题画风系统一起做（6 套：月/时/墨/仙/山海经/蓝色大肥鱼）②只做默认「月」主题，其他后置 ③不做主题 | 每套主题含 30+ 组 light/dark 色值 + 独立 logo 图。另有品牌名（sydf 叫「时月东方」）、logo 图、功德箱外链等资产需用户提供或自行设计 |

### C. 未开工大项

| # | 事项 | 现状 | 说明 |
|---|---|---|---|
| C1 | 全量 A/B/C/D 扫描报告 | **未开始** | mingyu 共 557 个 `.ts`；现有 `DEPENDENCY_SCAN_REPORT.md` 只覆盖 12 个文件且自述已过期，不可当现状 |
| C2 | 逐文件移植计划 | **未开始** | 依赖 C1 |
| C3 | 规则 3 的历法前置 | **未开始** | 任务书要求先移植 `calendar/true-solar-time.ts` 等 4 个文件再移植依赖它们的术式；但当前这 4 个文件是**手写实现**而非移植（见 B1） |
| C4 | D 类 REST 封装 `ApiClient` | **未开始** | 规则 4；目前也没确认 mingyu 里是否真有 D 类模块 |
| C5 | 大字表分片懒加载 | **未开始** | 需分片的：`qimen-patterns`(7500 行)、`yilin-pair-index`(968KB)、`chinaBirthPlaceTree`(912KB)、康熙字典 |
| C6 | 离线/联网双模架构 | **未开始** | 用户目标：离线可用一部分，联网完整使用。依赖 C4 |
| C7 | 其余术式移植 | **未开始** | 小六壬、塔罗已完成；mingyu 顶层 `divination/` 另有约 20437 行 |

---

### 截图比对：现状 vs sydf（用户于 2026-09 提供的两张实机截图）

用户提供了 sydf 的两张截图：**桌面/宽屏打开侧栏抽屉** 与 **移动端首页**。以下为据此得出的结论，A12–A16 / B7 / B8 均由本次比对产生。

**sydf 侧栏（图 1）实际结构**（此前只知道有 15 个路由，现在能看到分组与顺序）：

```
[logo] 时月东方                    [×]
       东方术数
─────────────────────────────
首页（选中态：accent 底 + 左侧竖线 + 右箭头）
排盘 / 合盘 / 灵签 / 小六壬 / 每日一卦 / 今日运势
传统黄历 / 居家风水 / 西方占卜 / 姓名与数字
诸葛神数 / 孔明神卦
─────────────────────────────
案例 / 设置
```

其中**合盘、每日一卦、今日运势、传统黄历、居家风水、姓名与数字、诸葛神数、孔明神卦**这 8 项，Flutter 侧目前连数据都还没有。

**关键结论：sydf 是「对话优先」，不是「功能卡片列表」**

图 2 的首页主体自上而下是：

1. 顶栏：`☰` + `专业人士▾／内置AI` + `添加案例` + `记录`
2. 今日运势条：`☀ 今日运势 / 小吉·今天重心在钱款，先确认合并记录·金钱合作优先` + 颜色圆点 + `›`
3. Hero：圆角 logo 图 + 「探索未来 解读术数」（后半句渐变紫）+ 胶囊按钮「♡ 功德箱」
4. **大片留白**
5. 浮动输入框：`写下问题，交给梅花易数` + 工具 chips（`梅花易数▾` `问题灵感` `补充信息`）+ 紫色圆形发送键
6. 底部小字：`生成内容完全基于 AI 模型的胡言乱语，不构成任何形式建议`

对比现有 Flutter 首页（[home_page.dart](lib/features/home/presentation/home_page.dart) 122 行）：一个 `AppBar` + 两张功能卡片 + 一张「后续迁移」占位卡。**两者不是同一种产品形态**——sydf 的功能入口在侧栏，首页只负责提问；Flutter 现在的首页是入口聚合，没有对话位。

这解释了为什么之前觉得「像同一个设计语言但不像同一个 App」：**设计 token 复刻对了，但信息架构（IA）完全不同**。

**同时修正一处此前的判断**：上一轮说「小六壬那 4 个文件的暗色皮肤是内部不一致」，这个判断仍然成立（它确实不吃主题切换），但要注意 **sydf 的默认深色观感本身就是深色底 + 紫色 accent**（图 1 即深色侧栏）。所以 Flutter 那边的问题**不是「用了深色」，而是「用了另一套金色 + 墨蓝、且绕开了主题系统」**。修的时候应往 `AppTheme` 的深色槽位收敛，而不是简单删掉深色。

**sydf 的 6 套画风主题**（`divinationTheme.ts`）：`月`(清雅紫月) / `时`(明亮粉彩) / `墨`(水墨) / `仙`(清雅仙山) / `山海经`(古籍异兽) / `吃白饭的蓝色大肥鱼`(清透亮蓝)。每套含 30+ 组 `[light, dark]` 色值，为默认「月」这套已与 `AppTheme` 逐 token 对应。主题还带独立 logo 图与 `browserColor`。


---

## 第 3 轮：案例 + 快照 + 算法版本（产品地基）

**背景**：SYDF 文档 §14 第一阶段第 2/3/4 项与 §15 验收标准第 4 条要求
「案例库 + 案例快照 + 算法版本」，此前一件没做。本轮按先前建议的范围完成：
**只做模型和字段，不动存储层**，先跑通语义再换数据库。

### 本次修改

**新增**

- `lib/core/models/case_profile.dart`：`CaseProfile` + `CaseSnapshot`
  + `CaseGender` / `CaseCalendarType`
- `lib/features/cases/data/case_repository.dart`：案例 CRUD，存储键
  `zhaoxingzhai.cases.v1`
- `lib/features/cases/case_selection.dart`：全局案例选择（对应 sydf `caseSelection`）
- `lib/features/cases/presentation/cases_page.dart`：案例页（列表 / 选择 / 新建 / 编辑）
- 测试：`test/core/models/case_profile_test.dart`、
  `test/features/cases/case_repository_test.dart`

**修改**

- `DivinationHistoryRecord` 新增顶层 `algorithmId` / `algorithmVersion` /
  `schemaVersion` / `caseSnapshot` 四个字段
- `addXiaoliuren` / `addTarot` 新增可选 `caseSnapshot` 参数
- `AppShell`：`AppView.cases` 由历史页改为案例页；历史改为独立 endDrawer；
  小六壬与塔罗回调带上当前案例快照
- 历史卡片显示关联案例与算法版本（`algorithmLabel`）

### 关键设计决策（后续勿推翻）

1. **快照是值拷贝，不是引用**。修改或删除案例不影响已有历史，已有测试钉住
   （含「重新读盘后依然不变」）。
2. **快照反序列化不回退到当前案例**。字段缺失一律取缺省值。这是 SYDF §6.2
   最后一条的硬要求，违反会导致旧历史被后来的资料静默篡改。
3. **`birthDateTime` 存墙上时间 + `timezoneId`**，不存折算好的 UTC 瞬间。
   这样用户日后修正时区，录入的原始资料不会被悄悄改写。
4. **旧数据兼容**：顶层版本字段缺失时从 `payload.meta` / `payload.algorithm`
   回补；两者都没有时记为 `unknown` / `0`，**不假装是 v1**。
5. **历史与案例彻底分离**：案例页不展示任何结果，历史在 endDrawer
   （对齐 sydf「全局历史作为独立抽屉或路由状态」）。

### 验证

| 项 | 结果 |
|---|---|
| `flutter analyze` | 零问题 |
| `flutter test` | 103 全通过（原 68 + 新增 35） |
| 快照不受案例后续修改影响（含重新读盘） | ✅ 测试覆盖 |
| 旧数据版本回补 / 完全无版本标记为 unknown | ✅ 测试覆盖 |
| 选中案例被删除后选择状态自动失效 | ✅ 测试覆盖 |

### 下一步

1. **提交本轮改动** —— 工作区仍积压第 3 轮之前未提交的改动，多会话并行有覆盖风险
2. 数据导出流水线 + 三山国王灵签（A1 / A2）
3. 阻塞决策 B1（历法）/ B3（AGPL）/ B7（AI 是否主轴）仍未拍板

---

## 第 2 轮：修复 Web 平台上的 32 位乘法精度问题

**背景**：用户问「改的是否参考 mingyu 算法且能在 Flutter 中实现」。验证时发现第 1 轮的修复**在 Web 上失效**。

### 本次修改

文件：`lib/core/shared/random.dart`、`test/core/shared/random_test.dart`

**问题**：第 1 轮把 `_imul` 简化成了一行：

```dart
int _imul(int a, int b) => (a * b) & 0xFFFFFFFF;   // ❌ Web 上错误
```

这在原生平台正确（Dart int 为 64 位），但 **Web 上 Dart 的 int 由 JS 数字承载，只有 53 位精度**。
两个 32 位数乘积可达 2⁶⁴，远超 2⁵³，乘法**静默丢精度**。

实测同一份代码：

| 平台 | `seed="test"` 首个样本 | 与上游一致 |
|---|---|---|
| 原生 VM | `0.7171058997` | ✅ |
| Web (dart2js) | `0.1832452207` | ❌ |

即：**若出 Web 版/PWA，同一账号在手机与浏览器上会用同一种子抽到不同的卦。**

**修复**：回到 16 位半字分解，并**补上原实现漏掉的项**：

```dart
int _imul(int a, int b) {
  final ah = (a >> 16) & 0xffff;
  final al = a & 0xffff;
  final bh = (b >> 16) & 0xffff;
  final bl = b & 0xffff;
  return (al * bl + (((ah * bl + al * bh) & 0xffff) << 16)) & 0xFFFFFFFF;
}
```

`ah * bh` 项的真实权重是 2³²，在 32 位截断中必然为 0，故不参与计算也不会引入误差。
任一中间结果都不超过 2⁵³，因此在 Web 与原生上行为一致。

### 验证证据

| 验证项 | 结果 |
|---|---|
| 7 个种子 × 5 样本，原生 vs Web | **逐位一致** |
| 20000 组随机 32 位对，原生 vs Web | **逐字符一致** |
| Dart 结果 vs `Math.imul` | 20000 组 0 处不一致 |
| Dart 结果 vs BigInt 精确值 | 20000 组 0 处不一致 |
| `Math.imul` vs BigInt 精确值（基准自检） | 20000 组 0 处不一致 |
| 边界 `0xFFFFFFFF*0xFFFFFFFF` | = 1 ✅（旧版 Web 返回 0） |
| 7 个种子 vs 上游 TS（`npx tsx` 实跑） | 35/35 全吻合 |
| `flutter build web --release` | 通过 |
| wasm 编译 | 通过（未执行，无 wasmtime） |

### 测试补充（`random_test.dart` 30 → 32 条）

- 「种子哈希应正确截断超过 2^53 的乘积」—— 用 `0xFFFFFFFF * 16777619 ≈ 7.2e16` 这个高危输入钉住 Web 回归
- 「长种子（多轮哈希累乘）应保持确定性」—— 覆盖多轮累乘与中文种子

### 下一步（按优先级）

1. **数据导出流水线 + 三山国王灵签**（推荐的下一件事）
   - 新建 `scripts/export-mingyu-data.mjs`，改写 mingyu 的生成脚本
   - 输出到 `assets/data/<术式>/`
   - 灵签数据在 `ssgw-data/signs-full.ts`(1475 行) 与 `signs-01/02/03.ts` 两处都有，**导出前先确认哪个 authoritative**
   - 选它是因为纯查表、零精度风险，用来验证整条链路

2. **验证 `scripts/export_data.js` 能否导出全部 18 种牌阵**（1 分钟即可确认，见潜在问题 3）

3. **补 golden 向量到塔罗/小六壬端到端**
   - 同一种子下，Dart 抽牌序列应可完全复现
   - 塔罗洗牌 = 77 次 `randomInt`，是最能暴露随机层问题的用例

4. **`lib/core/models/` 不存在**
   - 目录约定里写了它，但代码里没有 → 文档与代码不一致
   - 要么建，要么从约定里删掉

5. **塔罗算法位置错误**
   - `lib/features/tarot/tarot_divination.dart`（1286 行）应移到 `lib/core/engine/tarot/`
   - 该文件内 `TarotKeywords`（约 989 行处）是硬编码关键词表，违反规则 1（应走 JSON）

6. **历法 1232 行手写代码待决**
   - `true_solar_time.dart`(425)、`civil_time.dart`(255)、`historical_timezone.dart`(239)、`china_dst.dart`(125)、`date_validation.dart`(121)、`date_utils.dart`(67)
   - 仅 4 个测试。与规则 3（统一改用 `lunar`，不要自己实现历法）冲突
   - 两个选择：删掉改用 `lunar`，或补齐上游测试（`true-solar-time.test.ts` 9 例、`civil-time.test.ts` 4 例）

### 潜在问题

1. **⚠️ 随机层已两次变更输出** —— 第 1 轮和第 2 轮各改了一次，同一种子在不同版本下结果不同。若任何分支/真机上存过带 seed 的历史记录，**无法再 replay**。当前无真实用户数据。
   - **建议**：给随机层加一个 `algorithmVersion` 常量并写入历史记录，以后再改就能识别旧数据。

2. **⚠️ 平台差异是本项目的一类系统性风险** —— 本轮暴露的不是笔误，而是「Web 的 int 只有 53 位」这个平台语义差异。后续移植中凡涉及**大整数位运算、哈希、校验和**的地方都需同样在 Web 目标下验证。`flutter test` **默认跑在原生 VM 上，测不出 Web 问题**，这是当前 CI 的盲区。
   - **建议**：关键的数值模块补 `dart compile js` + 对比的验证脚本，或在 CI 加 web 目标测试。

3. **`docs/DATA_EXPORT_SUMMARY.md` 已过期且可能有实质隐患** —— 文中写塔罗只有 3 种牌阵，实际 `tarot.json` 已有 18 种。且该文件疑似记录了**手工誊抄**的数据。若 18 种牌阵是后来手工补进去的，重跑 `scripts/export_data.js` 会把它们冲掉。**未验证**，动数据前先跑一次脚本 + `git diff`。

4. **`random.dart` 头注释已改** —— 从「完整移植自 mingyu-core」改为「设计参考…不追求位级一致」。但**当前实现实际与上游逐位一致**（已验证 35/35），注释里未承诺这一点。若希望长期保持一致，应改回注释并加比对脚本。

5. **4 个并行会话编辑同一工作树** —— 曾检测到 peer session `newproject-23/-d3/-71/-52`。开工前先 `git status` 确认无他人未提交改动。

6. **AGPL-3.0-only 传染性未决** —— mingyu 是 AGPL，本地直译算法会产生衍生作品。若未来要闭源发行需要用户决策（法律问题，非技术问题）。网络 API 调用通常不构成衍生作品。

7. **上游版本二选一未决** —— sydf 依赖 `mingyu-core 0.2.3`，本地 mingyu 是 `0.4.0`，baseline 文档 pin 的是 0.4.0。见 TODO B2。

8. **`codeUnitAt` vs `charCodeAt` 在代理对字符上可能分叉（未验证）** —— `_hashSeed` 逐码元哈希，Dart 的 `codeUnitAt` 与 JS 的 `charCodeAt` 都是 UTF-16 码元，BMP 内字符等价；但 emoji 等增补平面字符由代理对构成，两者行为是否一致**尚未实测**。若未来允许用户用 emoji 当种子，可能出现同种子跨平台不同结果。见 TODO A9。

### 避免重复劳动（给并行会话）

以下分析**已完成**，勿重做：
- mingyu `divination/` 分层结构已摸清：`rules/data`(常量) → `algorithms`(算术) → `evidence`(渲染) → `prompt`(AI)
- `algorithms/` 共 21798 行；其中 `qimen/helpers/` 约 7500 行是格局数据表，真正算术约 3500 行
- `evidence` 层体量最大且本质是 UI 渲染（如 `meihua` 算术 567 行 vs evidence 1897 行），**建议不移植**，改为 Flutter 渲染
- 数据体量分级已量：单文件直载 ≤138KB（灵签/诸葛签），必须分片懒加载的有 `qimen-patterns`(7500 行)、`yilin-pair-index`(968KB)、`chinaBirthPlaceTree`(912KB)、康熙字典
- `generate-zhuge-signs.mjs` 是**从外部 URL 抓网页**的，不能离线跑；但其产物 `zhuge-signs.ts`(1928 行) 已提交，导出脚本应读产物而非重抓

以下验证**已做过**，勿重做：
- `random.dart` 的 `_imul` / `_hashSeed` / `createSeededRandom` 已在原生 + Web 双平台验证通过
- `randomInt` 已跑卡方检验（300 万样本，p 值 0.22–0.99），确认无模偏差
